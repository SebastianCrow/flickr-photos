import { ElementOnScreenTriggerDirective } from './element-on-screen-trigger.directive';

describe('ElementOnScreenTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementOnScreenTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
